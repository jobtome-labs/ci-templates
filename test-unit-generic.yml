---
include:
  - remote: "https://raw.githubusercontent.com/jobtome-labs/ci-templates/v3.17.0/templates/test-unit.yml"

test:unit:
  extends: .test:unit

  image: "${CI_REGISTRY_IMAGE}/${APP_TEST_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"

  variables:
    GIT_STRATEGY: none

  script:
    - echo "<RUN_UNIT_TESTS_COMMAND>"
