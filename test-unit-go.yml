---
include:
  - remote: "https://raw.githubusercontent.com/jobtome-labs/ci-templates/chore/terraform-tofu-compatibility/templates/test-unit.yml"

test:unit:go:
  extends: .test:unit

  image: "${CI_REGISTRY_IMAGE}/${APP_TEST_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"

  script:
    - go test ./...
