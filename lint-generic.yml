---
include:
  - remote: "https://raw.githubusercontent.com/jobtome-labs/ci-templates/v3.32.1/templates/lint.yml"

lint:
  extends: .lint

  image: "${CI_REGISTRY_IMAGE}/${APP_LINT_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"

  variables:
    GIT_STRATEGY: none

  script:
    - echo "<RUN_LINTING_COMMAND>"
