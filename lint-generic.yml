---
include:
  - remote: "https://raw.githubusercontent.com/jobtome-labs/ci-templates/chore/terraform-tofu-compatibility/templates/lint.yml"

lint:
  extends: .lint

  image: "${CI_REGISTRY_IMAGE}/${APP_LINT_IMAGE_NAME}:${CI_COMMIT_SHORT_SHA}"

  variables:
    GIT_STRATEGY: none

  script:
    - echo "<RUN_LINTING_COMMAND>"
